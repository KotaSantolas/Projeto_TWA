doctype html
html(lang='pt')
  head
    meta(charset='UTF-8')
    meta(name='viewport', content='width=device-width, initial-scale=1.0')
    title= title || 'Gestão Barbearia'
    
    //- Google Fonts (opcional mas recomendado)
    link(href='https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap', rel='stylesheet')
    
    //- Font Awesome (para os ícones)
    link(rel='stylesheet', href='https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css')
    
    //- CSS Principal
    link(rel='stylesheet', href='/css/style.css')
    
  body
    if currentUser
      nav.navbar
        .container
          h1.logo Barbearia 
            if userType === 'barbeiro'
              span(style='font-size: 0.75rem; color: #3498db; margin-left: 0.5rem;') ✂️ Barbeiro
            else if userType === 'cliente'
              span(style='font-size: 0.75rem; color: #2ecc71; margin-left: 0.5rem;') 👤 Cliente
          ul.nav-links
            li: a(href='/') Dashboard
            
            //- APENAS BARBEIROS veem estas opções
            if userType === 'barbeiro'
              li: a(href='/clientes') Clientes
              li: a(href='/barbeiros') Barbeiros
              li: a(href='/servicos') Serviços
            
            //- AMBOS veem Reservas
            li: a(href='/reservas') Reservas
            
            li: a(href='/auth/logout') Sair
    
    main.container
      if error
        .alert.alert-error= error
      
      block content
    
    script(src='/js/main.js')
